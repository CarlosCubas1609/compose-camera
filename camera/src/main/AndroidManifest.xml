<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Permissions required by the camera library.
        Declared here so consuming apps don't need to add them manually.

        READ_MEDIA_IMAGES + READ_MEDIA_VIDEO con maxSdkVersion="33" para Play Store compliance.
        En Android 14+ (API 34+) no se solicitan ni usan; en su lugar la galería usa el
        Photo Picker del sistema (PickMultipleVisualMedia) que no requiere permisos.
        El carousel en API 34+ consulta sólo archivos capturados por esta app (OWNER_PACKAGE_NAME).
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_IMAGES"
        android:maxSdkVersion="33" />
    <uses-permission
        android:name="android.permission.READ_MEDIA_VIDEO"
        android:maxSdkVersion="33" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

</manifest>

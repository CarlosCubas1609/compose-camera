<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Permissions required by the camera library.
        Declared here so consuming apps don't need to add them manually.

        READ_MEDIA_IMAGES + READ_MEDIA_VIDEO + READ_MEDIA_VISUAL_USER_SELECTED are declared
        together WITHOUT maxSdkVersion so that on Android 14+ (API 34+) the system shows the
        three-way dialog: "Allow all photos" / "Select photos" / "Don't allow".
        - "Allow all"     → READ_MEDIA_IMAGES + READ_MEDIA_VIDEO granted → full gallery access
        - "Select photos" → READ_MEDIA_VISUAL_USER_SELECTED granted → picker-scoped access
        Without all three in the manifest, "Allow all" is never offered and the gallery stays empty.
    -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VISUAL_USER_SELECTED" />

</manifest>
